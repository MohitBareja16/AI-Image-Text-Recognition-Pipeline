# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt — DecodeLabs Project 4: AI Recognition Pipeline
# Python 3.8+ required. Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Core Computer Vision ──────────────────────────────────────────────────────
opencv-python==4.8.1.78       # Image I/O, pre-processing, DNN inference module

# ── OCR Engine Wrapper (Path 1) ───────────────────────────────────────────────
pytesseract==0.3.10           # Python wrapper for Google Tesseract OCR
                              # NOTE: Requires Tesseract binary installed separately
                              # See docs/SETUP.md Step 4

# ── Image Processing Utilities ────────────────────────────────────────────────
Pillow==10.0.1                # Image format support and fallback loading
numpy==1.24.4                 # Array operations (required by opencv)
imutils==0.5.4                # Convenience: deskew, resize, display helpers
pandas==2.0.3                 # DataFrame handling for pytesseract Output.DATAFRAME

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==7.4.3
pytest-cov==4.1.0
